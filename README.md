# Mastermind

Mastermind is a Dockerized orchestrator app with easy configuration of event driven architectures. Similar to AWS step functions, state machines, and temporal workflows, but with a focus on simplicity and ease of use. It allows you to define your workflows in a simple YAML/JSON format, and then execute them with a single command.
